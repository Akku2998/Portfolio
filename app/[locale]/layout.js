import "./globals.css";
import { Raleway, Open_Sans } from "next/font/google";
import { CustomLayout } from "./components";
import i18nConfig from "@/i18nConfig";
import { dir } from "i18next";
import initTranslations from "@/i18n";
import TranslationsProvider from "@/app/[locale]/context/translationProvider";
import { DarkModeProvider } from "./context/darkModeProvider";

const i18nNamespaces = ["home"];

const raleway = Raleway({
  subsets: ["latin"],
  variable: "--font-raleway",
});

const openSans = Open_Sans({
  subsets: ["latin"],
  variable: "--font-open-sans",
});

export const metadata = {
  metadataBase: new URL("https://portfolio-swart-kappa-35.vercel.app"),

  title: {
    default: "Nidhi Kumari | Full Stack Developer",
    template: "%s | Nidhi Kumari",
  },

  description:
    "Nidhi Kumari is a Full Stack Developer with 2+ years of experience building web applications using React, Next.js, Node.js, Express, Prisma and MySQL.",

  keywords: [
    "Nidhi Kumari",
    "Full Stack Developer",
    "React Developer",
    "Next.js Developer",
    "Node.js Developer",
    "JavaScript Developer",
    "MERN Developer",
    "Bengaluru Developer",
  ],

  authors: [
    {
      name: "Nidhi Kumari",
    },
  ],

  creator: "Nidhi Kumari",

  openGraph: {
    title: "Nidhi Kumari | Full Stack Developer",

    description:
      "Full Stack Developer with 2+ years of experience building web applications using React, Next.js, Node.js, Express, Prisma and MySQL.",

    url: "https://portfolio-swart-kappa-35.vercel.app",

    siteName: "Nidhi Kumari Portfolio",

    type: "website",

    locale: "en_IN",
  },

  robots: {
    index: true,
    follow: true,
  },
};

export function generateStaticParams() {
  return i18nConfig.locales.map((locale) => ({ locale }));
}

export default async function RootLayout({ params: { locale }, ...rest }) {
  const { resources } = await initTranslations(locale, i18nNamespaces);

  return (
    <html
      lang={locale}
      dir={dir(locale)}
      className={`${raleway.variable} ${openSans.variable} sans-serif`}
    >
      <body>
        <TranslationsProvider
          namespaces={i18nNamespaces}
          locale={locale}
          resources={resources}
        >
          <DarkModeProvider>
            <CustomLayout {...rest} />
          </DarkModeProvider>
        </TranslationsProvider>
      </body>
    </html>
  );
}

// import "./globals.css";
// import { Raleway, Open_Sans } from "next/font/google";
// import { CustomLayout } from "./components";
// import i18nConfig from "@/i18nConfig";
// import { dir } from "i18next";
// import initTranslations from "@/i18n";
// import TranslationsProvider from "@/app/[locale]/context/translationProvider";
// import { DarkModeProvider } from "./context/darkModeProvider";

// const i18nNamespaces = ["home"];

// const raleway = Raleway({
//   subsets: ["latin"],
//   variable: "--font-raleway",
// });

// const openSans = Open_Sans({
//   subsets: ["latin"],
//   variable: "--font-open-sans",
// });

// export const metadata = {
//   title: "Create Next App",
//   description: "Generated by create next app",
// };

// export function generateStaticParams() {
//   return i18nConfig.locales.map((locale) => ({ locale }));
// }

// export default async function RootLayout({ params: { locale }, ...rest }) {
//   const { resources } = await initTranslations(locale, i18nNamespaces);
//   return (
//     <html
//       lang={locale}
//       dir={dir(locale)}
//       className={`${raleway.variable} ${openSans.variable} sans-serif`}
//     >
//       <body>
//         <TranslationsProvider
//           namespaces={i18nNamespaces}
//           locale={locale}
//           resources={resources}
//         >
//           <DarkModeProvider>
//             <CustomLayout {...rest} />
//           </DarkModeProvider>
//         </TranslationsProvider>
//       </body>
//     </html>
//   );
// }
